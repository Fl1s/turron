server:
  port: 8092

spring:
  application:
    name: extraction-service
  threads:
    virtual:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update

management:
  endpoints:
    web:
      exposure:
        include: prometheus