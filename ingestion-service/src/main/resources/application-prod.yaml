spring:
  kafka:
    bootstrap-servers: kafka:9092

eureka:
  instance:
    hostname: ingestion-service.turron.svc.cluster.local
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://eureka-server:8095/eureka/