apiVersion: apps/v1
kind: Deployment
metadata:
  name: hashing-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hashing-service
  template:
    metadata:
      labels:
        app: hashing-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8093"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      containers:
        - name: hashing-service
          image: ghcr.io/fl1s/turron/hashing-service:v1.2
          ports:
            - containerPort: 8093
          resources:
            requests:
              cpu: "300m"
              memory: "384Mi"
            limits:
              cpu: "600m"
              memory: "512Mi"
          envFrom:
            - configMapRef:
                name: hashing-service-config
            - secretRef:
                name: hashing-service-secret
      initContainers:
        - name: wait-for-eureka
          image: busybox
          command:
            - sh
            - -c
            - |
              until nc -z eureka-server 8761; do
                echo "Waiting for Eureka server..."
                sleep 5
              done