apiVersion: apps/v1
kind: Deployment
metadata:
  name: eureka-server
  namespace: turron
spec:
  replicas: 1
  selector:
    matchLabels:
      app: eureka-server
  template:
    metadata:
      labels:
        app: eureka-server
    spec:
      containers:
        - name: eureka-server
          image: ghcr.io/fl1s/eureka-server:latest
          ports:
            - containerPort: 8095
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
      imagePullSecrets:
        - name: ghcr-secret
---
apiVersion: v1
kind: Service
metadata:
  name: eureka-server
  namespace: turron
spec:
  selector:
    app: eureka-server
  ports:
    - port: 8095
      targetPort: 8095
