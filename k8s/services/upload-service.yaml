apiVersion: apps/v1
kind: Deployment
metadata:
  name: upload-service
  namespace: turron-app
  labels:
    app: upload-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: upload-service
  template:
    metadata:
      labels:
        app: upload-service
    spec:
      containers:
        - name: upload-service
          image: ghcr.io/fl1s/turron/upload-service:latest
          ports:
            - containerPort: 8091
          envFrom:
            - configMapRef:
                name: turron-config
          env:
            - name: SPRING_DATASOURCE_URL
              value: jdbc:postgresql://postgres:5432/uploads
            - name: EUREKA_INSTANCE_HOSTNAME
              value: upload-service
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8091
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8091
            initialDelaySeconds: 15
            periodSeconds: 20
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: upload-service
  namespace: turron-app
spec:
  ports:
    - port: 8091
      targetPort: 8091
  selector:
    app: upload-service
